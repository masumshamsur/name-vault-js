# Node.js / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json  # Optional: ignore if you prefer yarn.lock only
# .env files (for secrets like MONGO_URI)
.env
.env.local
.env.development
.env.production

# Logs and databases
*.log
.DS_Store  # macOS-specific

# Build/output
/dist
/build

